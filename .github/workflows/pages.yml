name: Deploy GitHub Pages

# Dispara em todo push pro branch `prod` (produção). Push em `main` é só
# staging local e não atualiza o Pages.
on:
  push:
    branches: [prod]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Permite só 1 deploy concorrente; cancela em-curso ao chegar novo
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: prod

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload dashboard folder
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dashboard

      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
